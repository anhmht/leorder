import {
  push, replace, goForward, goBack,
} from 'connected-react-router';

export const routerPush = push;

export const routerReplace = replace;

export const routerGoForward = goForward;

export const routerGoBack = goBack;
